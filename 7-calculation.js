const num1 = 5
const num2 = 10

function addValues(){
    console.log(`the sum is : ${num1 + num2}`)
}

addValues()

// if there is a function inside the module that is invoked that code will run 
// even though its not assigned a variale